<% @coupons.each do |coupon| %>
    <div><%= coupon.store %></div>
    <div><%= link_to coupon.coupon_code, coupon_path(coupon.id) %></div>
<% end %>

<%= link_to "create a new coupon?", new_coupon_path %>